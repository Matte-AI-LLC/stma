{
  "name": "stma",
  "private": true,
  "version": "0.19.0",
  "license": "Elastic-2.0",
  "description": "STMA — Speak to my Agent. AgentOps for teams that build with coding agents: one map of every run, collision warnings, team rules in every session, handoffs and evidence",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20.6"
  },
  "scripts": {
    "dev": "npm run dev -w @matteai/stma-server",
    "build": "npm run build -w @matteai/stma-server && npm run build -w @matteai/stma",
    "start": "npm run start -w @matteai/stma-server",
    "test": "npm run test -w @matteai/stma-server",
    "demo:local": "npm run test -w @matteai/stma-server -- --reporter=verbose",
    "demo:agents": "npm run test -w @matteai/stma-server -- test/agent-lab.test.ts --reporter=verbose && npm test --prefix examples/payments-api && npm test --prefix examples/storefront-web",
    "human-lab:check": "node scripts/human-lab-prompt.mjs validate && node --test scripts/human-lab-prompt.test.mjs && npm test --prefix examples/payments-api",
    "human-lab:prompt": "node scripts/human-lab-prompt.mjs",
    "machine-lab:check": "node --test scripts/lab-provision.test.mjs",
    "typecheck": "npm run typecheck -w @matteai/stma-server && npm run typecheck -w @matteai/stma",
    "version:set": "node scripts/set-version.mjs",
    "version:check": "node scripts/check-version.mjs",
    "cli": "npm run dev -w @matteai/stma --",
    "db:generate": "npm run db:generate -w @matteai/stma-server"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "typescript": "^7.0.2"
  }
}
